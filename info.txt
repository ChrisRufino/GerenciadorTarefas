Depende do que você quer expor via API — nem toda model precisa virar recurso HTTP. Hoje você tem:

users → users-controller.ts ✅
sessions (login) → sessions-controller.ts ✅
tasks → tasks-controller.ts (create/index) + tasks-status-controller.ts (update status) ✅
Ainda faltam controllers/rotas pra:

teams — criar/listar equipes (teams-controller.ts)

team_members — adicionar/remover usuário de uma equipe (pode ser um teams-members-controller.ts ou 
até rotas aninhadas em teams-routes.ts,tipo POST /teams/:id/members)

tasks_history — normalmente esse aqui não precisa de POST manual, porque faz mais sentido 
ser preenchido automaticamente 

(ex: dentro do próprio tasksStatusController.update, quando o status muda, 
você grava um registro no histórico). 

Não deveria ser um endpoint que o cliente chama diretamente.

Se for criar, posso ajudar a estruturar o TeamsController (create/index)

e o vínculo de membros — quer que eu já monte 

isso ou prefere fazer e eu vou revisando conforme você for escrevendo, como fizemos até agora?

tasks history sei que nao precisa, mas o teams e team members precisa, entao criarei e 
depois voce ver se está correto
Show, faz sentido. Cria o teams-controller.ts (create/index) e o 
de team_members (provavelmente um add/remove de membro) 
com as rotas correspondentes, 
e quando terminar me chama que eu reviso — igual fizemos com o tasks-status-controller.ts,
 olhando erro por erro se aparecer algo.